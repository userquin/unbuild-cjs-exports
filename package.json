{
  "name": "unbuild-check-named",
  "packageManager": "pnpm@9.15.4",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf packages/*/dist --glob && pnpm -r run build",
    "test:attw": "pnpm -r test:attw"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.17.3",
    "mlly": "^1.7.4",
    "rimraf": "^6.0.1",
    "rollup": "^4.31.0",
    "typescript": "^5.7.3",
    "unbuild": "3.1.0"
  }
}
